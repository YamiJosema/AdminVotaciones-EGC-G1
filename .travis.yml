language:
    - java

services:
    - mysql

jdk:
    - oraclejdk8
    - openjdk7

before_install:
    - mysql -u root < initialize.sql

script:
    - mvn clean test
    - ls -lah

cache:
    directories:
        - $HOME/.m2  

dist:
    - trusty

sudo:
    - false

notifications:
    - email: true
